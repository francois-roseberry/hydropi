export const waterTemperatureReducer = (state = INITIAL_WATER_TEMPERATURE_STATE) => {
  return state;
}

const INITIAL_WATER_TEMPERATURE_STATE = { data: [{ x: 0, y: 22 }, { x: 1, y: 21 }, { x: 2, y: 19 }, { x: 3, y: 20 }] };
